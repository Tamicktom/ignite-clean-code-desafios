//* Booleans

const isUserOnMajority = true;

// is, does, has, should, can, will, etc.

const disabled = false;
const isDisabled = true;

const userHasGroup = true;

if (isDisabled) {
  console.log('The user is disabled');
}
